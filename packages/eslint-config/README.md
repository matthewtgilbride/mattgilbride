# `eslint-config-mgilbride`

Shared eslint configuration

TODO: figure out why dependencies need to be installed here instead of using peerDependencies

## Usage

```
module.exports = {
  extends: "@mattgilbride",
  parserOptions: {
    project: "tsconfig.json",
  },
}
```
